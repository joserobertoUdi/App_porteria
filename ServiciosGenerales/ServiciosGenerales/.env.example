# Variables de entorno del backend (copie este archivo a su entorno real).
# En Windows puede definirlas con: setx VARIABLE "valor"  o  $env:VARIABLE="valor"
# Los valores que empiezan con "CAMBIAR" son obligatorios en producción.

# ---- Base de datos (SQL Server) ----
DB_CONNECTION_STRING=Server=localhost;Database=UniversidadDB;Trusted_Connection=True;TrustServerCertificate=True

# ---- JWT (obligatorios en producción) ----
JWT_KEY=CAMBIAR_POR_UNA_CLAVE_SEGURA_DE_AL_MENOS_32_CARACTERES
JWT_ISSUER=UDI
JWT_AUDIENCE=app-universidad
JWT_EXPIRE_MINUTES=120

# ---- Seed inicial (opcional) ----
# Solo crea el administrador si SEED_ADMIN_PASSWORD está definida.
SEED_ADMIN_PASSWORD=CAMBIAR_POR_UNA_CONTRASENA_ADMIN_SEGURA
SEED_ADMIN_NOMBRE=Administrador
SEED_ADMIN_DOCUMENTO=admin
SEED_ADMIN_TIPO_USUARIO_ID=1
SEED_ADMIN_ROL_ID=1

# Usuarios de demostración (porteros). Deshabilitar en producción.
SEED_USUARIOS_DEMO=false
SEED_PORTERO_PARQUEO_PASSWORD=CAMBIAR
SEED_PORTERO_PORTERIA_PASSWORD=CAMBIAR

# ---- SharePoint (fase futura, opcional) ----
SHAREPOINT_BASE_URL=
